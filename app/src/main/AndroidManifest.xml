<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.votenote.net">
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- @TODO Remove 'android:usesCleartextTraffic="true"' ("We are using http now")-->
    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:screenOrientation="portrait"
        android:supportsRtl="true"
        android:theme="@style/Theme.VoteNote">
        <activity
            android:screenOrientation="portrait"
            android:name=".ui.SplashScreenActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:screenOrientation="portrait"
            android:name=".ui.auth.AuthActivity"
            android:configChanges="orientation"
            android:label="@string/title_activity_login"

            android:theme="@style/Theme.VoteNote.Fullscreen"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:screenOrientation="portrait"
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.VoteNote.NoActionBar">
        </activity>
    </application>

</manifest>